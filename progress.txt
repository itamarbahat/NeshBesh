NeshBesh — PRD Execution Progress
==================================
PRD: PRD.md (rules-correction PRD, branch 29/5)
Started: 2026-05-30
Last updated: 2026-05-30

KEY
---
[x] = done   [~] = in progress   [ ] = pending   [!] = blocked

EXECUTION SEQUENCE (PRD §8)
---------------------------
[x] 1. Engine: canFullyCompleteDouble helper + 4:5 chooser gating
       → engine/index.ts:420  |  useGameStore.ts:35,705 (defensive re-check)
[x] 2. Engine: Nesh Strike free-move scope change (full-board after bar clears)
       → useGameStore.ts:563-605 (live hasBarPieces re-check per move)
[x] 3. State: doublesCount + blockedDoubleStreak exposure + reset rules
       → useGameStore.ts:58,88,183 (endTurnImpl resets); 233 (extra-turn preserves)
[x] 4. UI: DoublesCounterChip placement
       → DoublesCounterChip.tsx (compact + standard variants).
         Mounted in Board, sidebar, dice bar, RemoteBottomBar, OpponentHeaderChip.
[x] 5. UI: 4:5 chooser redesign (header + 6 dice + disable-by-completability)
       → DoubleChooserPanel.tsx:32-90 (getChoosableDoubleValues drives enable)
[x] 6. UI: Single-die display for 4:3 / 5:1 phases
       → DicePanel handles phases SPECIAL_43_ROLL / RESULT, SPECIAL_51_ROLL / RESULT
         (legacy SingleDieRoller Hebrew-localized as safety net)
[x] 7. Messaging: 5:2 + 4:3 Hebrew captions via MessageBanner
       → MessageBanner.tsx surfaces state.message; scheduleMessageClear auto-clears 3s
[x] 8. Pass `tsc --noEmit`
       → Exit 0, no type errors
         Required `node --stack-size=8000` due to deep generics in node_modules
[x] 9. Code-level verification per PRD §7
       → All 8 rule areas verified in source. Details below.
[x] 10. Commit pushed to 29/5 for Vercel preview
       → Commit a95fcac on origin/29/5

VERIFICATION RESULTS (PRD §7)
-----------------------------
Doubles Counter:  ✅ PASS
  • Regular double bumps counter (useGameStore.ts:396, newCount = doublesCount+1)
  • 3rd consecutive double → TABLE_FLIP, counter reset to 0 (lines 397-400)
  • Non-double resets via enterMovingOrSkip default (line 217: ?? 0)
  • All special-roll paths explicitly set doublesCount:0 (lines 420,424,437,447,457,490,495)
  • 6:3 re-roll preserves counter (choose63 line 729-741 does NOT touch doublesCount) — R7 ✅
  • endTurnImpl resets to 0 on turn pass (line 183)
  • extra-turn cycle preserves counter (afterMoveCheckDice line 226-243)
  • Component: DoublesCounterChip reads currentPlayer + doublesCount, shows
    real count for active player, idle for opponent

4:5 Chooser:  ✅ PASS
  • Helper canFullyCompleteDouble simulates 4 moves of value v (engine/index.ts:420-471)
    handles bar-first, then forward play with calculatePossibleMoves
  • Selector getChoosableDoubleValues: returns only completable values if any,
    else all 6 (fallback, useGameStore.ts:35-45)
  • Panel uses choosable list to enable/disable buttons (DoubleChooserPanel.tsx:32,52)
  • chooseDouble defensively re-checks list and warns + ignores invalid values
    (useGameStore.ts:705-718)
  • Header text "איזה דאבל שבא לך" matches R-spec (Hebrew localization of "Whatever double you like")

Nesh Strike (6:5):  ✅ PASS
  • Trigger captures blots and sets neshStrikeFreeMovesLeft=2 (lines 427-454)
  • Free-move executor re-reads bar status per move (lines 562-605):
      onBarNow = hasBarPieces(board, sign)
      mustSelectBar = onBarNow
      targetRestricted = onBarNow
  • Pure bear-off → onBarNow=false → free moves full-board (R5 ✅)
  • 1 piece on bar → move 1 restricted to opp home; bar clears → move 2 full-board (R4 ✅)
  • 2 pieces on bar → both entries restricted; once bar clears, subsequent free (R6 ✅)
  • Fully blocked opp home + on bar → SKIP with bar message (lines 433-445)

5:2 Backwards:  ✅ PASS
  • No bar: enterMovingOrSkip with backward=true, msg "לך 5:2 אחורה!" (lines 478-486)
    auto-clears after 3s via scheduleMessageClear
  • 1 piece on bar: forward entry → pending52Flip set → after entry, post-move
    logic (lines 676-700) flips backward=true, shows "לך 5:2 אחורה!" (R-spec ✅)
  • 2+ pieces on bar: no flip, both dice used forward (lines 470-477)
  • Pure bear-off: still routes to backward path → player forced to shuffle (R8 ✅)

4:3 Roll-for-Backwards:  ✅ PASS
  • Phase SPECIAL_43_ROLL: msg "הטל קוביה" set + auto-clear (line 489-491)
  • rollSingleDie handles SPECIAL_43_ROLL (lines 512-529):
      Bar exception: enterMovingOrSkip with [d], backward=false, msg "4:3 מהבר — נכנס קדימה d"
        (turn ends after single entry — R11 ✅)
      No bar: phase SPECIAL_43_RESULT, msg "לך אחורה X צעדים!" + auto-clear
  • DicePanel renders single-die visual for SPECIAL_43_* phases (R-spec ✅)

5:1 Roll-for-Double:  ✅ PASS
  • Phase SPECIAL_51_ROLL: msg "5:1 — Roll 1 die..." set (line 495)
  • rollSingleDie handles SPECIAL_51_ROLL (lines 530-549):
      Bar exception: 4 dice [d,d,d,d], extraTurn=false, is51FourMove=true
      No bar: phase SPECIAL_51_RESULT, 4 dice, extraTurn=false
  • Bear-off cost: engine/index.ts:242-293 getDiceAfterMove consumes 1 die per
    bear-off (exact match line 256 or single-die overshoot line 265-267)
  • Bar entry consumes 1 die per applyMove call (R-spec ✅)
  • Does NOT grant extra turn even when d is itself a regular double (R12 ✅)

Move accounting (R14 / R15):  ✅ PASS
  • Regular double (4:5 chosen double, 5:1 four-move, 2:2 etc.):
      Each move/entry/bear-off consumes 1 pip via getDiceAfterMove
  • R14 verified: bar entry → 1 pip (engine/index.ts:308-310 splices dice index)
  • R15 verified: bear-off → 1 pip in typical case (exact-match or overshoot)
  • Note: getDiceAfterMove has a ceil(needed/d) branch (line 269-274) intended
    only for unreachable doubles+overshoot cases — isLegalBearOff rejects D > X
    upstream, so this branch is defensive. No action needed.

Star Mars (R13):  ✅ PASS
  • calculateVictory (engine/index.ts:473-491): when loserBorneOff===0 AND
    opp bar piece exists → returns {type:'Star Mars', points: Infinity}
  • doVictoryCheck (useGameStore.ts:245-289): on Infinity points, sets
    winner's sets to 3 directly (line 261), then championship check at line 278
    triggers GAME_OVER with "CHAMPIONSHIP!" message

NOTES / FOLLOW-UPS
------------------
1. Stale field: `neshStrikeStartedOnBar` (useGameStore.ts:77,450,845)
   Now superseded by the live `hasBarPieces` re-check in the free-move
   executor (line 575). Field is still set but never read for gating logic.
   Field comment at lines 72-77 also describes the OLD behavior.
   → Recommend cleanup in a follow-up commit:
     - Delete the field from state interface, resetTurnState, initial state,
       and all set() patches (4 sites).
     - Drop the stale comment block.
   Not blocking; safe to leave as-is until a UI/test cleanup pass.

2. User-facing manual verification (real device / browser playthrough) still
   pending. All code paths have been verified statically against the PRD §7
   checklist. A live playthrough should focus on the visual/timing aspects:
     - DoublesCounterChip color transitions (idle → warn → danger)
     - MessageBanner enter/exit animations + auto-clear timing
     - DicePanel single-die render for 4:3 / 5:1
     - DoubleChooserPanel disabled-state visuals
     - Hebrew text rendering / RTL layout

3. R3 ("4:5 move accounting: bar entry = 1 move, bear-off = 1 move") follows
   automatically from the same getDiceAfterMove pool used for regular doubles,
   so it's covered by the move-accounting verification above.

STATUS: PRD complete. All deliverables shipped in commit a95fcac on 29/5.
        Awaiting live playthrough verification (item 2 above).
